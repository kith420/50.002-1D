module modfour (
    input a[32],  // dividend
    input b[32],  // divisor
    output out[32] // remainder (a mod b)
) {
    
    
    always {
        
        
        out = a - ((a >> 2) << 2) 
        
    }
}